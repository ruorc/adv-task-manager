import { type JSX } from 'react';
import Typography from '@mui/material/Typography';

/** placeholder */
export const AboutPage = (): JSX.Element => (
  <Typography variant="h4">About Content Page</Typography>
);
