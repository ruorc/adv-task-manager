import { type JSX } from 'react';
import Typography from '@mui/material/Typography';

/** placeholder */
export const TaskDetailPage = (): JSX.Element => (
  <Typography variant="h4">Task Detail Page</Typography>
);
