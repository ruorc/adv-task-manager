import { type JSX } from 'react';
import Typography from '@mui/material/Typography';

/** placeholder */
export const BoardDetailPage = (): JSX.Element => (
  <Typography variant="h4">Board Detail Page</Typography>
);
