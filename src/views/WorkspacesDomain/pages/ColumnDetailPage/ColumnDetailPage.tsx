import { type JSX } from 'react';
import Typography from '@mui/material/Typography';

/** placeholder */
export const ColumnDetailPage = (): JSX.Element => (
  <Typography variant="h4">Column Detail Page</Typography>
);
