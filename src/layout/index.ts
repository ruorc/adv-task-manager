/**
 * Composition Root Barrel Export for the Structural Application Layout Module.
 * Consolidates and exposes the primary application layout wrapper component.
 */

export { Layout } from './Layout';
