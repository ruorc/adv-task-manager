/**
 * Composition Root Barrel Export for the Routes Registry Module.
 * Consolidates and exposes routing path constants and type definitions.
 */

export { ROUTES, routeHelpers } from './routes';

export type { AppRoutePattern } from './routes';
